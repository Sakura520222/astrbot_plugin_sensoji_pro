{
    "enable_change_fortune": {
        "description": "是否启用转运功能",
        "type": "bool",
        "default": true,
        "hint": "关闭后用户将无法使用转运功能"
    },
    "max_change_fortune_times": {
        "description": "每日最大转运次数（0表示不限制）",
        "type": "int",
        "default": 3,
        "hint": "设置用户每日可转运的最大次数，设为0则不限制次数"
    },
    "daily_cleanup": {
        "description": "自动清理时间间隔（默认1天）",
        "type": "int",
        "default": 1,
        "hint": "设置定时清理时间间隔"
    }
}